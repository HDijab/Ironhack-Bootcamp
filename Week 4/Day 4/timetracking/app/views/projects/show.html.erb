<h1>Project: <%= @project.name %></h1>
<p><%= @project.description %></p>
<p>Total time spent on project in the last month: <%= @hours %> Hours - <%= @minutes %> Minutes</p>
<%= link_to("Previous", project_path("#{@project.id - 1}")) %>
<%= link_to("Next", project_path("#{@project.id + 1}")) %>
<%= link_to("Show Entries", project_entries_path(@project)) %>