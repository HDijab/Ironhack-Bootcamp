<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
      <title></title>
  </head>
  <body>
    <h1><%= @username %></h1>
    <label id="hours"><%= Time.now.hour %></label>:<label id="minutes"><%= Time.now.min %></label>:<label id="seconds"><%= Time.now.sec %></label>
    <h1> <%= @hello %> </h1>
    <% if Date.today.tuesday?%>
    <p>
    <h2><%= @greeting %></h2>
    Izzy Ironside is <strong>NOT</strong>
    a fan of Mondays like yesterday.
    </p>
    <% else %>
    <h1>Today is <%= Date.today %></h1>
    <% end %>
    <% if @order == 'ago' %>
      <h2><%= @order %></h2>
      <h2><%= Time.now.hour - @time %></h2>
    <% elsif @order == 'in_the_future' %>
      <h2><%= @order %></h2>
      <h2><%= Time.now.hour + @time %></h2>
    <% end %>
    <img src=http://i110.photobucket.com/albums/n101/fhatchet/monkey.jpg />
    <script src="/time.js" type="text/javascript"></script>
  </body>
</html>